export default function Confirmation() {
  //const name = localStorage.getItem("firstName") || "Guest ";

  return (
    <div className="text-info mt-4">{`Thanks for coming, . Your attendance is confirmed`}</div>
  );
}
